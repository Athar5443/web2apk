<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!-- Base application theme. -->
    <style name="Theme.Web2APK" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Primary brand color (Sleek Dark Gray). -->
        <item name="colorPrimary">@color/monochrome_dark</item>
        <item name="colorPrimaryVariant">@color/black</item>
        <item name="colorOnPrimary">@color/white</item>
        <!-- Status bar color. -->
        <item name="android:statusBarColor">@android:color/transparent</item>
        <item name="android:windowLightStatusBar">true</item>
    </style>

    <style name="Theme.App.Starting" parent="Theme.SplashScreen">
        <!-- Set the splash screen background -->
        <item name="windowSplashScreenBackground">@color/white</item>
        <!-- Use the launcher icon as the splash icon -->
        <item name="windowSplashScreenAnimatedIcon">@mipmap/ic_launcher</item>
        <!-- Post-splash theme -->
        <item name="postSplashScreenTheme">@style/Theme.Web2APK</item>
    </style>

    <color name="monochrome_dark">#212121</color>
    <color name="monochrome_light">#FAFAFA</color>
    <color name="white">#FFFFFFFF</color>
    <color name="black">#FF000000</color>
</resources>